# painter
A Painting Company Website.

## Using an existing local repository

If you already have this repository on your machine, use that copy directly:

```bash
cd /path/to/your/painter-repo
git pull origin <branch-name>
```
